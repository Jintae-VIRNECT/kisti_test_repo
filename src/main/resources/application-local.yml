spring:
  application:
    name: content-server
  datasource:
    url: jdbc:mysql://192.168.99.100:3306/contents
    username: root
    password: pwd
    hikari:
      minimum-idle: 2
      max-lifetime: 30000
      maximum-pool-size: 5
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    properties:
      hibernate:
        format_sql: true
        show_sql: true
  servlet:
    multipart:
      max-file-size: 300MB
      max-request-size: 400MB
  cloud:
    loadbalancer:
      ribbon:
        enabled: false

logging:
  level:
    com.virnect: DEBUG
    org.hibernate.type.descriptor.sql: trace

eureka:
  instance:
    prefer-ip-address: true
  client:
#    enabled: false
    service-url:
        defaultZone: http://localhost:8761/eureka

## Server Configuration
server:
  port: 8078

# File Upload Configuration
upload:
  dir: "upload/"
  serverUrl: "http://localhost:${server.port}/contents/"
  allowed-extension: '.ares,.mars,.Ares,.Mars'

process:
  serverUrl: "http://localhost:8079/processes"

content:
  serverUrl: "http://localhost:${server.port}/contents"

user:
  serverUrl: "http://192.168.6.3:8081/users"

## FEIGN Configuration
feign:
  okhttp:
    enabled: true

## AWS Configuration
cloud:
  aws:
    credentials:
      access-key: AKIAIYFYOR4X6LM3YKSQ
      secret-key: 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
    region:
      auto: true
    s3:
      bucket:
        name: "virnect-platform-qa"
        resource: "workspace/"
