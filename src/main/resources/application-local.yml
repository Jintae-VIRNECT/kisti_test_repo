spring:
  application:
    name: WorkspaceServer
  datasource:
    url: jdbc:mysql://localhost:3306/smic?characterEncoding=UTF-8&serverTimezone=UTC
    username: root
    password: qjsprxm0!
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    properties:
      hibnerate:
        format_sql: true


logging:
  level:
    org.hibernate.type.descriptor.sql: trace


## Server Configuration
server:
  port: 8082
  servlet:
    context-path: "/workspaces"


## Project Configuration
file:
  upload-path: "upload/"
  url: "http://localhost:${server.port}"
  allow-extension: "gif,jpg,jpeg,png"

# User Server Configuration
user:
  serverUrl: "http://localhost:8081/users"
