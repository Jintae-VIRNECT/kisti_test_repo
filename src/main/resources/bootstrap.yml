server:
  port: 8079

spring:
  application:
    name: process-server
  cloud:
    config:
      uri: ${CONFIG_SERVER}
      profile: ${VIRNECT_ENV}
      name: ${spring.application.name}
  profiles:
    active: ${VIRNECT_ENV}