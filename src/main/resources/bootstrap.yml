server:
  port: 8082

spring:
  application:
    name: workspace-server
  cloud:
    config:
      uri: ${CONFIG_SERVER}
      profile: ${spring.profiles.active}
      name: ${spring.application.name}