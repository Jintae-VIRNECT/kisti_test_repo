server:
  port: 8084

spring:
  application:
    name: message-server
  cloud:
    config:
      uri: ${CONFIG_SERVER}
      profile: ${VIRNECT_ENV}
      name: ${spring.application.name}
  profiles:
    active: ${VIRNECT_ENV}