server:
  port: 8084

spring:
  profiles:
    active: ${VIRNECT_ENV}
  application:
    name: message-server
  cloud:
    config:
      uri: ${CONFIG_SERVER}
      profile: ${VIRNECT_ENV}
      name: ${spring.application.name}
