## Server Configuration
server:
  port: 8321

spring:
  profiles:
    active: ${VIRNECT_ENV}
  application:
    name: user-server
  cloud:
    config:
      uri: ${CONFIG_SERVER}
      profile: ${VIRNECT_ENV}
      name: ${spring.application.name}
