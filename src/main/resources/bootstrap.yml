## Server Configuration
server:
  port: 8322

spring:
  profiles:
    active: ${VIRNECT_ENV}
  application:
    name: account-server
  cloud:
    config:
      uri: ${CONFIG_SERVER}
      profile: ${VIRNECT_ENV}
      name: ${spring.application.name}
