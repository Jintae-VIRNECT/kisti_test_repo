## Server Configuration
server:
  port: 8086

spring:
  application:
    name: download-server
  cloud:
    config:
      uri: ${CONFIG_SERVER}
      profile: ${spring.profiles.active}
      name: ${spring.application.name}
