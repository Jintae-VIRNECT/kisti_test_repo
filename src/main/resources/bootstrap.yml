server:
  port: 8761

spring:
  application:
    name: eureka-server
  cloud:
    config:
      uri: ${CONFIG_SERVER}
      profile: ${VIRNECT_ENV}
      name: ${spring.application.name}
  profiles:
    active: ${VIRNECT_ENV}
