server:
  port: 8073

spring:
  profiles:
    active: ${VIRNECT_ENV}
  application:
    name: gateway-server
  cloud:
    config:
      uri: ${CONFIG_SERVER}
      profile: ${VIRNECT_ENV}
      name: ${spring.application.name}
