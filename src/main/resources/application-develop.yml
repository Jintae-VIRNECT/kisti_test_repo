spring:
  application:
    name: content-server
  datasource:
    url: jdbc:mysql://192.168.6.3:4336/contents
    username: virnect-content
    password: virnect1234
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
  servlet:
    multipart:
      max-file-size: 300MB
      max-request-size: 400MB

#logging:
#  level:
#    org.hibernate.type.descriptor.sql: trace

eureka:
  instance:
    prefer-ip-address: true
  client:
    service-url:
      defaultZone: http://192.168.6.3:8761/eureka

## Server Configuration
server:
  port: 8078

# File Upload Configuration
upload:
  dir: "upload/"
  serverUrl: "http://121.162.252.234:18083/contents/"
  allowed-extension: '.ares,.mars,.Ares,.Mars'

process:
  serverUrl: "http://121.162.252.234:1${server.port}/process"

content:
  serverUrl: "http://121.162.252.234:1${server.port}/contents"

user:
  serverUrl: "http://121.162.252.234:18081/users"

## FEIGN Configuration
feign:
  okhttp:
    enabled: true
