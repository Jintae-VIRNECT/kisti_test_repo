spring:
  application:
    name: login-service
  thymeleaf:
    cache: false
    mode: HTML

eureka:
  instance:
    prefer-ip-address: true
  client:
    service-url:
      defaultZone: http://stgeureka.virnect.com/eureka


logging:
  level:
    com.virnect.login.service: DEBUG
