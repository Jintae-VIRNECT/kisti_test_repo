#Because it is called first. Bootstrap must be set properties up first.
server.port=8000

spring.application.name=remote-service-server

#active parameter must be set environment value
spring.profiles.active= ${VIRNECT_ENV}

spring.cloud.config.uri=${CONFIG_SERVER}
spring.cloud.config.profile=${VIRNECT_ENV}
spring.cloud.config.name=${spring.application.name}
