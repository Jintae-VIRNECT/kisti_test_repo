FROM node:alpine
ENV APP_HOME=/usr/app
ENV PORT=8887

WORKDIR $APP_HOME

COPY ./packages $APP_HOME/packages
COPY ./projects $APP_HOME/projects
COPY ./package.json $APP_HOME/package.json

RUN yarn

EXPOSE $PORT

CMD ["yarn", "workspace", "smic", "deploy"]






